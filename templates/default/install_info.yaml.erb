<%=
YAML.dump(JSON.parse({
  'install_method' => {
    'tool' => 'chef',
    'tool_version' => "chef-#{@chef_version}",
    'installer_version' => "datadog_cookbook-#{@cookbook_version}",
  },
}).to_json).to_s %>
