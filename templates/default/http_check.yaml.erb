<%# Sanitize the compiled Mash to standard Array/Hash objects by way of JSON -%>
<%= YAML.dump(JSON.parse(({ 'instances' => @instances, 'logs' => @logs }).to_json)).to_s %>

init_config:
# Nothing to configure here
